<h1>All epic restaurants 🍔</h1>

<%= link_to 'create a restaurant', new_restaurant_path  %>

<% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant)  %><%= '⭐️' * restaurant.rating %></p>
  <p><%= restaurant.address %></p>
<% end %>
